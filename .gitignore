.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep

# gphoto-libs: Windows camera runtime libraries (~7 MB total).
# Tracked deliberately to ensure zero-setup and reproducible builds across different Windows machines.